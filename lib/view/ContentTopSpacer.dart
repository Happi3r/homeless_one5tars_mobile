import 'package:flutter/material.dart';

class ContentSpacer extends StatelessWidget {
  const ContentSpacer({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 40, width: double.infinity,);
  }
}
